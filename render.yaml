services:
  - type: web
    name: cineverse-catalogo
    runtime: python
    plan: free
    buildCommand: ""
    startCommand: python server.py
    healthCheckPath: /api/health
    envVars:
      - key: OMDB_API_KEY
        sync: false
