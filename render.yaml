services:
  - type: web
    name: cineverse-catalogo
    runtime: python
    plan: free
    buildCommand: ""
    startCommand: python -m backend.server
    healthCheckPath: /api/health
    envVars:
      - key: OMDB_API_KEY
        sync: false
